#include <iostream>

#include "render.h"

Render::Render() {
    std::cout << "Render is initializing." << std::endl;
}

Render::Render(std::string path_to_scene) {
    std::cout << "Render is initializing." << std::endl;
}